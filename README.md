# Airlines-Delay-Prediction
Airlines Delay Prediction using Streamlit and scikit-learn. The project includes data preprocessing, model training (Random Forest), and a web app to predict if a flight will be delayed based on airline, airports, day, time, and flight duration.
